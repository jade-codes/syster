[test]
# Root test configuration for all web packages
preload = ["@testing-library/jest-dom", "./packages/viewer/test-setup.ts"]
coverage = true
coverageDir = "coverage"
coverageReporter = ["text", "lcov"]
